import component from './component';
import s from './Text.pcss';

export const Alpha = component(s.alpha, 'h1');
export const Beta = component(s.beta, 'h2');
export const Gamma = component(s.gamma, 'h3');
export const Delta = component(s.delta, 'h4');
export const Epsilon = component(s.epsilon, 'h5');
export const Big = component(s.big, 'span');
export const Small = component(s.small, 'span');
