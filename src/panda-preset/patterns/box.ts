import type { PatternConfig } from '@pandacss/types';

/**
 * Generic container with responsive props to control whitespace, layout,
 * positioning and colors.
 */
export const box: PatternConfig = {
	transform(props) {
		return props;
	},
};
